{
  "name": "gods-eye-uap",
  "private": true,
  "version": "0.1.0",
  "description": "God's Eye // UAP \u2014 a 3D globe intelligence console for Unidentified Anomalous Phenomena: documented encounters, reconstructed flight paths, official government footage and files, and reference imagery.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/fix-cesium-base.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "sync:official": "node scripts/sync-dvids.mjs",
    "build:bluebook": "node scripts/build-bluebook.mjs",
    "build:nuforc": "node scripts/build-nuforc.mjs",
    "verify:media": "node scripts/verify-media.mjs",
    "data": "npm run sync:official && npm run build:bluebook && npm run build:nuforc && npm run build:airspace && npm run build:geipan && npm run build:mufon && npm run build:journals && npm run verify:media",
    "build:airspace": "node scripts/build-airspace.mjs",
    "build:mufon": "node scripts/build-mufon.mjs",
    "build:geipan": "node scripts/build-geipan.mjs",
    "build:journals": "node scripts/build-journals.mjs"
  },
  "dependencies": {
    "@mapbox/vector-tile": "^3.0.0",
    "astronomy-engine": "^2.1.19",
    "cesium": "^1.145.0",
    "pbf": "^5.1.2",
    "satellite.js": "^6.0.2"
  },
  "devDependencies": {
    "vite": "^6.4.3",
    "vite-plugin-cesium": "^1.2.23",
    "vitest": "^5.0.1"
  }
}
